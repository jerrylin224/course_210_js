var numbers = [1, 3, 4, 6, 7];
var lastNumber = 4;

if (numbers.indexOf(lastNumber) >= 0) {
  console.log('The last number ' + lastNumber + ' appears.');
} else {
  console.log('The last number ' + lastNumber + ' did not appear.');
}

